If you want to make notifications, you will have to require the script modules called "notification", it is located in a modules file in "mk_roleplay"


Then if you want to make a notification, you must create it with the line below:
require_notification.create_notif(player, "message", Color3.fromRGB(0, 0, 0))
